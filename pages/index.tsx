import type { NextPage } from "next";
import HeroContainer from "../components/HeroContainer";

const Home: NextPage = () => {
  return (
    <div>
      <HeroContainer>Opa</HeroContainer>
    </div>
  );
};

export default Home;
